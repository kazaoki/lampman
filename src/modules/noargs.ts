
'use strict'

import libs = require('../libs');

/**
 * 引数なしのとき
 */

export default function noargs(commands: any, options: any, config: object) {

    // libs.d(config)

    // show docker images



}
